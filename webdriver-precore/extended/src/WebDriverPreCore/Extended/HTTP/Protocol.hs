
module WebDriverPreCore.Extended.HTTP.Protocol
  ( 
    -- shared types from the base protocol
    module WebDriverPreCore.HTTP.Protocol
  )
where

import WebDriverPreCore.HTTP.Protocol (Command (..))
